import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";  // THIS MUST MATCH THE FILE NAME EXACTLY

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);